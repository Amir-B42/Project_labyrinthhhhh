#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <assert.h>

//idées : 

typedef int** lab; //matrice classique

lab create(int p, int q){
    lab labyrinthe = malloc(sizeof(int*)*q);
    for(int i=0;i<len;i++){
        lab[i] = calloc(sizeof(int)*p)
    }
    return ;    
}

bool is_real(lab ){
    
}




























